# Kusogarch - Default Application Keybindings (Immutable)
# App launchers are now in the user layer (~/.config/hypr/bindings.conf).
# This file is kept for reference but no longer sourced by default.
