#!/bin/bash
# kusogarch-cmd-colorpicker - Pick a color from the screen

COLOR=$(hyprpicker -a 2>/dev/null)

if [ -n "$COLOR" ]; then
    echo "$COLOR" | wl-copy
    notify-send "Color Picker" "Copied: $COLOR"
fi
