# Kusogarch - Environment Variables (User Overrides)
# Defaults are in ~/.local/share/kusogarch/defaults/hypr/envs.conf
# Add your own environment overrides below.

# Uncomment for NVIDIA:
# env = LIBVA_DRIVER_NAME,nvidia
# env = __GLX_VENDOR_LIBRARY_NAME,nvidia
# env = GBM_BACKEND,nvidia-drm
