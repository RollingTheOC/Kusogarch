# Kusogarch - Window Rules (User Overrides)
# Defaults are in ~/.local/share/kusogarch/defaults/hypr/windows.conf
# Add your own window rules below.

# Example: force an app to a specific workspace
# windowrule = workspace 5, match:class ^(discord)$
