# Kusogarch Default Theme - Lock Screen Colors
# These values are referenced by the main hyprlock.conf

# This file provides theme-specific overrides for hyprlock
# The actual lock screen layout is in config/hypr/hyprlock.conf
